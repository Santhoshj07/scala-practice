object absolute {
  def test(x:Int) : Int =
    {
    val abs_Diff = Math.abs(x - 71)
    if (x > 71) 3 * abs_Diff else abs_Diff
    }
     
   def main(args: Array[String]): Unit = {
      println("Result: " + test(40));
      println("Result: " + test(20));
   }
}
